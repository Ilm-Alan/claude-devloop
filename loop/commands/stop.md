---
description: "Stop the active loop"
allowed-tools: ["Bash(${CLAUDE_PLUGIN_ROOT}/scripts/stop-loop.sh)"]
---

# Stop Loop

```!
${CLAUDE_PLUGIN_ROOT}/scripts/stop-loop.sh
```

Confirm the loop has been stopped.
